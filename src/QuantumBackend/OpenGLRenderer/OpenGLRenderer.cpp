#include <QuantumCore/Renderer/Renderer.hpp>
#include <glad/gl.h>
#ifdef QUANTUM_GLFW_WINDOW
static_assert(false && MUST INCLUDE WINDOW AFTER RENDERER!);
#endif
namespace Quantum
{
    class Renderer::RenderImpl
    {

    };
} // namespace Quantum
